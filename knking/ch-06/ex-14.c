#include <stdio.h>

int main(void) {
    if (n % 2 == 0);
        printf("n is even\n");

    if (n % 2 == 0)
    printf("n is even\n");

    // The semicolon on the first line creates a null statement -- meaning the print statement always prints
}
